const treeUtil = require('./tree.util');
const { TreeNode, buildTree, printTree } = treeUtil;

/**
 * @param {TreeNode} root
 * @return {TreeNode}
 */
var pruneTree = function(root) {
  //
};

// console.log(printTree(pruneTree(buildTree([1, null, 0, 0, 1]))));

console.log(buildTree([1, null, 0, 0, 1]));
